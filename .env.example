PORT=3000

# Se não informar DB_TYPE e NODE_ENV=development, o projeto usa SQLite (comportamento atual).
# Para usar Postgres (ex.: via Docker Compose), defina:
# DB_TYPE=postgres
DB_HOST=localhost
DB_PORT=5432
DB_USER=finance
DB_PASS=finance
DB_NAME=finance
